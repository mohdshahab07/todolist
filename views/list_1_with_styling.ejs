<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/styles.css">
    <!-- linking css file that present in public folder.  -->
</head>

<body>
    <div class="box" id="heading">

        <h1>
            <%=worklist%> list
        </h1>
    </div>

    <div class="box">

        <div class="item">

            <h1 class="h">
                <%=worklist%> list start from below
            </h1>
        </div>

        <% for(var i=0;i<additem.length;i++){ %>
            <form class="item" action="/delete" method="post">
            <div class="item">
                <input type="checkbox" id="ck" name="checkbox" value=<%= additem[i]._id %> onchange="this.form.submit()">
                <p>
                    <%= additem[i].name %>
                </p>
            </div>
            <input type="hidden" name="listtitle" value="<%= worklist %>">
            </form>
            <% } %>
                <!-- looping through "additem" array each time to show each item of a list in different line -->

          <% if(worklist==="Today" ){%>
             <form class="litem" action="/" method="post">
                <input type="text" name="newitem" placeholder=" Add new item" autocomplete="off">
                <button class="button" type="submit" name="button">+</button>
             </form>
         <%} else{%>
            <form class="litem" action="/customroute" method="post">
                <input type="text" name="newitem" placeholder=" Add new item" autocomplete="off">
                <button class="button" type="submit" name="rname" value= <%= worklist %> >+</button>
            </form>
         <% } %>

                            <!-- or -->

    <!-- <form action="/" method="post"> 
            <input type="text" name="newitem" placeholder=" Add new item" autocomplete="off">
             <button class="button" type="submit" name="button" value="<%=worklist%>">+</button>
         </form> -->
         <!-- in this we post the value of worklist at home route and after check at the server end weather the value is from home route or from "work" route and redirect to server accordingly. -->

    </div>

    <!-- <img src="afmc.jpeg" alt="image not dispalayed"> -->
    <!-- this image not showed as this image not present in public folder. -->

</body>

</html>